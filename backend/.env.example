# Backend Environment Configuration

# =====================================================
# Server Configuration
# =====================================================
# CORS Origins (automatically set by ServerManager, but can be overridden)
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

# Log Directory (automatically set by ServerManager)
LOG_DIR=./logs

# =====================================================
# OpenRouter Configuration (Optional)
# =====================================================
# These are used for OpenRouter API headers for better analytics
YOUR_SITE_URL=https://www.op.scrllwise.com/
YOUR_SITE_NAME=Scrollwise AI

# =====================================================
# Database Configuration
# =====================================================
# SQLite database will be created automatically in the current directory
# No additional database configuration needed for local setup

# =====================================================
# Vector Store Configuration
# =====================================================
# Qdrant vector database will be created locally
# Path: ./qdrant_db (automatically created)

# =====================================================
# Cache Configuration
# =====================================================
# LangChain cache directory (automatically created)
# Path: ./.cache (automatically created)

# =====================================================
# Development/Debug Settings
# =====================================================
# Set to 'development' for debug logging
ENVIRONMENT=production

# Rate limiting settings (optional overrides)
# GEMINI_PRO_RPM=60
# GEMINI_PRO_TPM=2000000
# GEMINI_FLASH_RPM=60
# GEMINI_FLASH_TPM=2000000